Spotify application to search artists :

A React Native  project

To run:

  $ npm install
  for iOS
  $ react-native ios
  for Android
  $ react-native android

You will need to install the iOS and/or Android SDK to run the apps.
